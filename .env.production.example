# Used by docker-compose.prod.yml for nginx vhost template.
PUBLIC_DOMAIN=www.example.com
ADMIN_DOMAIN=admin.example.com
API_DOMAIN=api.example.com
LETSENCRYPT_EMAIL=ops@example.com

# Keep this file on the VPS as `.env.production`.
# Also create `server/.env` on the VPS from `server/.env.production.example`.
